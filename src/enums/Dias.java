//package enums;

public enum Dias { //Se utiliza la keyword enum en vez de class
    /*Todos los elementos agregados a un enum son por defecto public, static y final
    por lo que son constantes automaticamente. El default datatype es String, asi que
    imprime el nombre de la variable pero en formato string*/
    LUNES, 
    MARTES,
    MIERCOLES,
    JUEVES,
    VIERNES,
    SABADO,
    DOMINGO
}
